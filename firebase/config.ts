// src/firebase.ts (or your firebase/config.ts)import { initializeApp } from 'firebase/app';import { getAuth } from 'firebase/auth'; // Or initializeAuth if you need specific persistence options// Your Firebase configuration objectconst firebaseConfig = {  apiKey: "YOUR_API_KEY",  authDomain: "YOUR_AUTH_DOMAIN",  projectId: "dormdate-14a3b", // Your project ID  storageBucket: "YOUR_STORAGE_BUCKET",  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",  appId: "YOUR_APP_ID",  measurementId: "YOUR_MEASUREMENT_ID" // If using Analytics};// Initialize Firebaseconst app = initializeApp(firebaseConfig);// Initialize Firebase Authentication and get a reference to the serviceexport const auth = getAuth(app); // This is the 'Auth' object that signInWithEmailAndPassword expects!// If you needed specific persistence (like indexedDBLocalPersistence), you'd use initializeAuth like this:// import { initializeAuth, indexedDBLocalPersistence } from 'firebase/auth';// export const auth = initializeAuth(app, { persistence: [indexedDBLocalPersistence] });